import{bV as o,aL as t,u as d,G as c}from"./chunks/framework.DvBUyM1S.js";const u=JSON.parse('{"title":"QCM V2 镜像页字段口径对齐清单（SCM & MDP）","description":"","frontmatter":{"id":"PB-20260825-001","type":"playbook","title":"QCM V2 镜像页字段口径对齐清单（SCM & MDP）","tags":["qcm-v2","protable","field-mapping","jackson"],"status":"verified","source":"conversation:2026-08-25","created":"2026-08-25T00:00:00.000Z","updated":"2026-09-03T00:00:00.000Z"},"headers":[],"relativePath":"playbooks/scm-mirror-field-alignment.md","filePath":"playbooks/scm-mirror-field-alignment.md"}'),a={name:"playbooks/scm-mirror-field-alignment.md"};function r(s,e,l,i,n,m){return t(),d("div",null,[...e[0]||(e[0]=[c('<h1 id="qcm-v2-镜像页字段口径对齐清单-scm-mdp-jp-ui-后端契约" tabindex="-1">QCM V2 镜像页字段口径对齐清单（SCM &amp; MDP / jp-ui + 后端契约） <a class="header-anchor" href="#qcm-v2-镜像页字段口径对齐清单-scm-mdp-jp-ui-后端契约" aria-label="Permalink to &quot;QCM V2 镜像页字段口径对齐清单（SCM &amp; MDP / jp-ui + 后端契约）&quot;">​</a></h1><blockquote><p>适用：外部系统（SCM/XLS/MDP 等）推送数据落库后，在 jp-ui 做只读镜像展示页（ProTable 列表）。 本文为可直接复用的落地核对清单，来源于 SCM采购入退库（scmPurchaseStock）两轮对齐实战（兄弟页面 scmPurchaseOrder 同口径），MDP 商品主数据（productMasterData）实战补充 1.1 变体。</p></blockquote><h2 id="_1-三条字段名口径-scm-实体固定模式" tabindex="-1">1. 三条字段名口径（SCM 实体固定模式） <a class="header-anchor" href="#_1-三条字段名口径-scm-实体固定模式" aria-label="Permalink to &quot;1. 三条字段名口径（SCM 实体固定模式）&quot;">​</a></h2><p>后端实体（如 <code>ScmPurchaseStock</code>）业务字段全部带 <code>@JsonProperty</code> 报文全大写注解（<code>billId→BILLID</code>、<code>whQty→WHQTY</code>），由此产生前端三条铁律：</p><table tabindex="0"><thead><tr><th>口径</th><th>规则</th><th>前端写法</th></tr></thead><tbody><tr><td>查询绑定参数</td><td>Spring MVC 绑定用驼峰（<code>@Query</code> 字段）</td><td>有搜索的列 <code>field</code> 取驼峰，保证搜索提交 key 能被绑定</td></tr><tr><td>响应 JSON key</td><td><code>@JsonProperty</code> 指定报文全大写</td><td>有搜索的列必须加 <code>formatter: ({ row }) =&gt; row.BILLID ?? &quot;&quot;</code> 从全大写 key 取值；无搜索的列 <code>field</code> 直接用全大写 key</td></tr><tr><td>非报文字段</td><td>无 <code>@JsonProperty</code>（派生字段、BaseEntity 审计字段 createUserName/createTime 等）</td><td>响应 key 即驼峰，<code>field</code> 直接用驼峰，无需 formatter</td></tr></tbody></table><h3 id="_1-1-mdp-变体-无-jsonproperty-驼峰实体-连续大写开头字段-jamsea-模式" tabindex="-1">1.1 MDP 变体：无 @JsonProperty 驼峰实体 + 连续大写开头字段（jAmsea 模式） <a class="header-anchor" href="#_1-1-mdp-变体-无-jsonproperty-驼峰实体-连续大写开头字段-jamsea-模式" aria-label="Permalink to &quot;1.1 MDP 变体：无 @JsonProperty 驼峰实体 + 连续大写开头字段（jAmsea 模式）&quot;">​</a></h3><p>MDP 镜像实体（如 <code>Product</code>）业务字段<strong>无</strong> <code>@JsonProperty</code>，响应 key 与驼峰字段同名，<code>field</code> 直接用驼峰即可；唯一例外是<strong>第二字符大写</strong>的字段（如季节 <code>jAmsea</code>），绑定 key 与响应 key 分裂成两套：</p><table tabindex="0"><thead><tr><th>层</th><th>推导规则</th><th>jAmsea 的 key</th></tr></thead><tbody><tr><td>响应 JSON（Jackson 序列化）</td><td>getter <code>getJAmsea()</code> 连续大写开头整体小写化</td><td><code>jamsea</code></td></tr><tr><td>GET 查询绑定（Spring MVC）</td><td>JavaBeans <code>Introspector.decapitalize</code>：前两字符均大写保留原样</td><td><code>JAmsea</code></td></tr></tbody></table><p>前端双 key 分离写法：列 <code>field: &quot;JAmsea&quot;</code>（搜索提交 key 能被绑定）+ <code>formatter: ({ row }) =&gt; row.jamsea ?? &quot;&quot;</code>（从响应 key 取显示值）。注意：QueryWrapperGenerator 已改为按 Field 反射取值，此类字段查询不再抛 500，失败症状是<strong>静默不生效</strong>，更难发现；若前端历史上改过字段名 key，先 <code>git log</code> 确认后端是否同步改过。</p><p><strong>mangle 判定细则与 jAmseaD 变体（2026-09-03 增补）</strong>：双 key 分裂不止 jAmsea 一例，判定看 getter 大写形态——「连续大写开头」触发 mangle 整体小写化（getJAmsea→jamsea）；<strong>词尾单大写不触发</strong>（getZsyearD→zsyearD，fallback 按 /[A-Z]/g 自动得 zsyear_d）；实体字段本身全小写则响应 key 与物理列天然一致（subseries 无需处理）。jAmseaD 变体：getJAmseaD() mangle 后为 jamseaD（首段小写化、词尾 D 保留），它到物理列 j_amsea_d 的距离 fallback 补不上——契约模式下 field 全用 snake_case 物理列名时，行数据归一化需显式 <code>ROW_KEY_ALIAS: { jamsea: &quot;j_amsea&quot;, jamseaD: &quot;j_amsea_d&quot; }</code>，回退查询参数绑定名仍是 JAmsea（Introspector 规则）另用 FALLBACK_PARAM_ALIAS 覆盖。</p><h2 id="_2-列集合对齐原则-列-聚合-sql-实际返回列" tabindex="-1">2. 列集合对齐原则：列 = 聚合 SQL 实际返回列 <a class="header-anchor" href="#_2-列集合对齐原则-列-聚合-sql-实际返回列" aria-label="Permalink to &quot;2. 列集合对齐原则：列 = 聚合 SQL 实际返回列&quot;">​</a></h2><p>列表接口若是<strong>聚合统计</strong>（自定义 Mapper XML 如 <code>selectAggregateList</code> 按 GROUP BY 维度分组），前端列集合必须等于聚合 SQL 的 SELECT 列，<strong>不是实体全字段</strong>：</p><ul><li>聚合维度字段保留（如按「入退库单号 bill_id」分组，则维度字段保留、明细行级字段全部删除——明细行号/尺码/商品编码等）；</li><li>数量类取 <code>SUM</code>，价格类可 <code>GROUP_CONCAT(DISTINCT ...)</code> 去重（多值逗号分隔展示），其余 <code>MAX</code> 取组内任一值；</li><li>聚合 SQL 不返回而实体存在的字段，配了列也恒空白，一律不配。</li></ul><h2 id="_3-搜索字段对齐原则-筛选项-实体-query-注解字段一一对应" tabindex="-1">3. 搜索字段对齐原则：筛选项 = 实体 <code>@Query</code> 注解字段一一对应 <a class="header-anchor" href="#_3-搜索字段对齐原则-筛选项-实体-query-注解字段一一对应" aria-label="Permalink to &quot;3. 搜索字段对齐原则：筛选项 = 实体 `@Query` 注解字段一一对应&quot;">​</a></h2><ul><li>实体上有 <code>@Query</code> 的字段才可配 <code>search: { component: &quot;input&quot; }</code>（默认模糊匹配）；</li><li>无 <code>@Query</code> 的字段（如 itemName、itemId）配了搜索也<strong>恒不生效</strong>（QueryWrapperGenerator 跳过），要么删搜索、要么先给后端补 <code>@Query</code>；</li><li>派生展示字段（如 <code>itemNameHandled</code> 去尺码段商品名称）通常非报文字段、无 <code>@Query</code>，只展示不筛选。</li></ul><h2 id="_4-商品名称派生字段模式" tabindex="-1">4. 商品名称派生字段模式 <a class="header-anchor" href="#_4-商品名称派生字段模式" aria-label="Permalink to &quot;4. 商品名称派生字段模式&quot;">​</a></h2><p>落库时由原始商品名称截掉逗号及右侧尺码段生成派生列（<code>item_name_handled</code>，如「跑鞋,34-42」→「跑鞋」），列表展示用派生值而非原始值：展示「单商品名称，不拼接尺码段」时取 <code>itemNameHandled</code>（响应 key 驼峰）。</p><h2 id="_5-新建镜像页核对清单" tabindex="-1">5. 新建镜像页核对清单 <a class="header-anchor" href="#_5-新建镜像页核对清单" aria-label="Permalink to &quot;5. 新建镜像页核对清单&quot;">​</a></h2><ol><li>读后端实体：标出 <code>@Query</code> 字段（搜索白名单）与 <code>@JsonProperty</code>（响应 key 口径）；无 <code>@JsonProperty</code> 时按 1.1 节检查连续大写开头字段的双 key 分裂；</li><li>读 Mapper XML 聚合 SQL：列集合以 SELECT 实际返回列为准，注意 SUM / GROUP_CONCAT / MAX 口径并在列注释标明；</li><li>ProTable 列定义按第 1 节三口径写 field / formatter；</li><li>菜单挂「主数据管理」一级菜单（menu_code=&#39;masterData&#39;），前端路径 <code>views/md/scmXxx/</code>、API <code>api/md/scmXxxService.js</code>、菜单 href <code>/md/scmXxx</code>（不存在「采购管理」一级菜单）；</li><li>列标题以用户提供的字段规格表为最终命名依据（如「类型」不写「类型编码」）。</li></ol><h2 id="_6-常见坑" tabindex="-1">6. 常见坑 <a class="header-anchor" href="#_6-常见坑" aria-label="Permalink to &quot;6. 常见坑&quot;">​</a></h2><ol><li>有搜索的列直接用全大写 field 或不加 formatter → 搜索失效或业务列全空白（绑定 key 与响应 key 是两套口径）。</li><li>按实体全字段配列而接口是聚合 SQL → 聚合不返回的列恒空白。</li><li>无 <code>@Query</code> 的字段配搜索 → 输入无任何过滤效果。</li><li>派生字段误当报文字段用全大写 key 取值 → 空白（非报文字段响应 key 是驼峰）。</li><li>连续大写开头字段（如 <code>jAmsea</code>）前端统一用序列化 key（<code>jamsea</code>）作 field → 显示正常但搜索提交 key 绑定不上，筛选静默失效（须按 1.1 节双 key 分离）。</li></ol><h2 id="_7-契约层-建模列-查询白名单-物理列三方一致性核对" tabindex="-1">7. 契约层：建模列 / 查询白名单 / 物理列三方一致性核对 <a class="header-anchor" href="#_7-契约层-建模列-查询白名单-物理列三方一致性核对" aria-label="Permalink to &quot;7. 契约层：建模列 / 查询白名单 / 物理列三方一致性核对&quot;">​</a></h2><blockquote><p>适用：页面走 pageSchema 契约（<code>sys_model_table</code> + <code>sys_model_table_column</code> + <code>sys_model_table_column_query</code> + <code>sys_menu_form_column.visible_columns</code>）。 来源：SCM备案明细（scmSupplierDetail）对抗式审查实战，详见 ERR-20260831-007。</p></blockquote><p><strong>物理列是权威</strong>，四份清单必须两两对齐：</p><table tabindex="0"><thead><tr><th>清单</th><th>位置</th><th>缺失后果</th></tr></thead><tbody><tr><td>物理列（权威）</td><td><code>CREATE TABLE</code> / 实体 / Mapper XML</td><td>—</td></tr><tr><td>建模列元数据</td><td><code>sys_model_table_column</code></td><td>页面拿不到契约，退回前端列 <code>search</code> 回退模式</td></tr><tr><td>查询白名单</td><td><code>sys_model_table_column_query</code></td><td>该字段不可筛选</td></tr><tr><td>展示列</td><td><code>sys_menu_form_column.visible_columns</code></td><td>该列不渲染</td></tr></tbody></table><p>核对动作（差集必须为空）：</p><ol><li><strong>契约列集合 − 物理列集合 = ∅</strong>：非空即 500 隐患。<code>PageSchemaConditionValidator</code> 只校验 白名单与 compareType，<strong>不校验 columnCode 是否对应真实物理列</strong>；<code>QueryWrapperGenerator.applyQueryConditions</code> 也是纯拼装（<code>queryWrapper.like(column, value)</code>）。缺陷会逃过编译期、启动期与&quot;不筛选直接打开页面&quot;的自测—— 不筛选时只表现为该列空白，只有输入筛选值才抛 MySQL 1054 Unknown column。</li><li><strong>物理列集合 − 契约列集合</strong>：剩下的要显式确认是&quot;有意预留&quot;还是&quot;落库但不展示的死列&quot;，不留中间态。</li><li>前端 <code>columns[].field</code> 与 <code>visible_columns</code> 逐项一致，顺序也随契约。</li></ol><p><strong>手写 Mapper XML 是第三处易漏点</strong>：<code>insertOrUpdateBatch</code> 的 INSERT 列、VALUES、<code>ON DUPLICATE KEY UPDATE</code> 三处都要同步，漏一处则物理列存在但永不落值（MyBatis-Plus 的自动填充救不了手写 XML）。</p><p><strong>回退模式陷阱</strong>：契约未加载时（典型如 <code>sys_menu_form_column.tenant_id</code> 与环境租户不匹配被判定&quot;未绑定&quot;）， 页面退回前端列 <code>search</code> + 实体 <code>@Query</code> 绑定，此时<strong>只有带 <code>@Query</code> 的字段能筛</strong>。契约里配了筛选、 实体却没 <code>@Query</code> 的字段静默失效——不报错、查不出差异，比 500 更难发现。新增契约筛选项时顺手确认实体有 <code>@Query</code>。</p><h2 id="_8-契约白名单静默收敛-页面有列-契约有列-2026-09-01-增补" tabindex="-1">8. 契约白名单静默收敛：页面有列 ≠ 契约有列（2026-09-01 增补） <a class="header-anchor" href="#_8-契约白名单静默收敛-页面有列-契约有列-2026-09-01-增补" aria-label="Permalink to &quot;8. 契约白名单静默收敛：页面有列 ≠ 契约有列（2026-09-01 增补）&quot;">​</a></h2><blockquote><p>来源：九页 tsv 对抗式审查实战（商品主数据 zsyear / 备案供应商股东 / 样品信息 12 列）。</p></blockquote><p>第 7 节四份清单之外还有第五份：<strong>前端 columns 数组</strong>。契约生效时 <code>applySchemaColumns</code> 按 <code>visible_columns</code> 白名单收敛列集合——页面写了列但契约（列元数据 + visible_columns）没登记，该列<strong>静默消失</strong>（不报错、不告警），契约恰好未加载时自测会直接漏掉。核对动作：<code>页面 columns field 集合 − visible_columns = ∅</code> 且顺序一致；契约新增列时同步三处：<code>sys_model_table_column</code>（sort 连续重排）、<code>visible_columns</code>（顺序 = 页面列顺序）、前端 columns。</p><p><strong>双向核查</strong>：权威口径表标记「删除」的字段，页面仍展示即违规——缺列要补，多列要删；物理列与契约列元数据可保留（历史数据不丢），仅从 visible_columns 与前端 columns 移除。</p><p><strong>码值列的「文本」筛选口径</strong>：枚举来自物理表注释或源系统时，新建系统字典（<code>sys_dict_type</code>/<code>sys_dict_value</code>，value=源码值），展示走字典翻译、筛选走码值 IN（契约 compare_type=IN + ref_code 指向字典），与既有字典列口径对齐；实体侧补 <code>@Query(type = QueryType.IN)</code> 保回退模式可用。注意：<code>ref_code</code> 指向字典的方式<strong>仅适用于系统字典</strong>，MDP 字典（<code>bd_mdp_dict</code>）走不通，见第 9 节。</p><p><strong>程序化比对法（2026-09-03 增补）</strong>：列规模上去后（86 列）肉眼核对 field 序列必漏——重排三列后忘插回新位置、前半替换后残留旧列块，两处均肉眼 diff 未见、靠脚本序列比对才暴露。核对动作固化：前端按行序提取 <code>field: &quot;</code> 出现序列，契约 visible_columns 提取 JSON 数组同序序列，<code>Compare-Object</code> 断言逐位相等（数量相同 + 顺序一致）；重排/扩列类修改完成后必跑，肉眼 diff 只作初审。</p><h2 id="_9-契约查询区的字典下拉退化与恢复-compare-type-in-前端-options-继承-2026-09-03-增补" tabindex="-1">9. 契约查询区的字典下拉退化与恢复：compare_type=IN + 前端 options 继承（2026-09-03 增补） <a class="header-anchor" href="#_9-契约查询区的字典下拉退化与恢复-compare-type-in-前端-options-继承-2026-09-03-增补" aria-label="Permalink to &quot;9. 契约查询区的字典下拉退化与恢复：compare_type=IN + 前端 options 继承（2026-09-03 增补）&quot;">​</a></h2><blockquote><p>来源：商品主数据（productMasterData）实战——契约生效后搜索区 MDP 字典多选下拉全部退化为 LIKE 文本输入框，用户须手输编码。</p></blockquote><p><strong>坑（两因素叠加，列表 formatter 翻译不受影响，只有搜索区退化）</strong>：</p><ol><li>pageSchema 契约生效后查询区完全由 <code>sys_model_table_column_query</code> 驱动，页面列上的 <code>search</code> 配置只在契约未生效时回退；</li><li>契约 DICT 选项链路（前端 <code>resolveRefOptions</code> → <code>dictUtils.getDictList</code>）只认<strong>系统字典</strong> <code>sys_dict_value</code>，认不了 MDP 字典 <code>bd_mdp_dict</code>。若码值列查询项配 LIKE 文本（当时因选项链路走不通而妥协），契约一生效字典下拉就消失。</li></ol><p><strong>修复模式（契约管可查性/位置/比较符，页面列定义仍是选项数据源——单一事实源）</strong>：</p><ol><li>SQL：码值列查询项 <code>compare_type</code> 配 <strong><code>IN</code></strong>（多值精确匹配，勿用 LIKE），<code>data_type</code> 保持 <code>TEXT</code>（DICT 链路走不通，不要硬配）；</li><li>前端：<code>queryToField</code>（<code>ProTable/utils.ts</code>）对 <code>compareType=IN</code> 的字段自动继承本地列 <code>search.options</code> 作为控件与选项来源（select + <code>mdpDictUtils.getMdpDictList(...)</code>）；本地未配 <code>options</code> 的字段（货号等自由文本 LIKE）不受影响；</li><li>后端零改动：<code>PageSchemaConditionValidator</code> 按 <code>compare_type</code> 白名单严格校验（不匹配抛「不支持的查询条件」），<code>QueryWrapperGenerator.applyQueryConditions</code> 原生支持 IN 多值（<code>QueryCondition.value</code> 是 <code>List&amp;lt;String&amp;gt;</code>，GET 经 qs indices 序列化）。</li></ol><p><strong>注意</strong>：<code>ModelMetaQueryService</code> 查询配置有 <strong>5 分钟本地缓存</strong>，改库后需等缓存过期或重启后端才能在 pageSchema 下发中生效。</p><p><strong>诊断提醒</strong>：用户报「字典回显有问题」先澄清具体现象（列表码值显原编码 vs 搜索区下拉变文本框 vs 下拉空选项 vs 切语言丢失）——列表翻译与搜索区选项是两条独立链路，混着排查浪费时间。</p><p><strong>例外分支：Y/N 通用枚举复用现成系统字典 commonYN，前端直接写（2026-09-03 二次增补，同日用户两轮纠错终审定版）</strong></p><blockquote><p>来源：同页「下单状态 isorder」值域 Y/N——用户先明确「取通用字典直接写就行了」，又纠错「yes_no 匹配不上」，最后找到现成字典「commonYN_Y」。</p></blockquote><ol><li><strong>先搜现成字典，再谈新建（两轮返工的核心教训）</strong>：配通用枚举前先 grep 前端 <code>getDictList\\(&quot;</code> 找同值域现成类型——项目里 Y/N 值域的现成字典是 <strong><code>commonYN</code></strong>（i18nKey 形如 <code>commonYN_Y/N</code>，供应商主数据详情页 isIntegratedFactory/isImport/isOdm 等 Y/N 字段在用），isorder 直接复用；<strong>不要只查 db DML 与基础库 dump</strong>——运行库的增量字典不在脚本里（commonYN 在 jeeplus-boot-saas-v3.sql 与 db/ 下都搜不到），自建 <code>md_yes_no</code> 后发现 commonYN 已删；</li><li><strong>通用字典值域必须核验</strong>：全局 <code>yes_no</code> 的 value 是 <strong>1/0</strong>（<code>sys_dict_value</code> id=5 是→1、id=6 否→0），<strong>不是 Y/N</strong>——用它配 Y/N 字段，formatter 匹配不上回退显原码，下拉选中提交 <code>&#39;1&#39;</code> 去 IN 查 <code>&#39;Y&#39;</code> <strong>静默空结果</strong>（比报错更难发现）；</li><li><strong>SQL 只改查询配置</strong>：<code>compare_type=&#39;IN&#39;</code>；列元数据保持 <code>TEXT</code>/<code>ref_code=NULL</code>——本地 <code>search.options</code> 本就必配（回退兜底），契约生效时 IN 继承覆盖契约 options，配 DICT+ref_code 等于白配；</li><li><strong>前端直接写</strong>：formatter <code>dictUtils.getDictLabel(&quot;commonYN&quot;, value, value)</code>（第三参回退原值）、options <code>dictUtils.getDictList(&quot;commonYN&quot;)</code>——纯读登录缓存，无需预加载；与 MDP 字典码值列同一条「TEXT + IN + 本地 options 继承」链路，仅选项源不同。</li><li><strong>详情页同步适用（2026-09-03 三次增补）</strong>：详情页（detail.vue）码值字段默认原值直出，同页「下单状态」显 Y/N 原码同样被用户要求翻译。通用机制：字段配置加可选 <code>dictType</code>（系统字典类型码），渲染统一走 <code>renderField</code>——配了 <code>dictType</code> 的 <code>dictUtils.getDictLabel(type, raw, raw)</code> 翻译（未命中回退原码）、空值统一 <code>-</code>，未配的原值直出；<code>commonYN</code> 纯读登录缓存无需预加载。以后详情页其他码值字段要翻译只需加配置——但 MDP 字典字段（bd_mdp_dict）勿照抄此机制：getDictLabel 只认系统字典，MDP 码需扩展异步预加载后走 mdpDictUtils。</li><li><strong>详情页全量字典翻译（2026-09-03 四次增补）</strong>：列表有的详情也要——详情页 87 字段按<strong>实体注释为权威</strong>全量配码：注释标「字典 XXX」的配 <code>mdpDictType</code>（41 个分类走 mdpDictUtils，onMounted <code>await loadMdpDicts([...])</code> 后再请求详情，清单为列表页的全量超集，模块缓存跨页面复用不重复请求）、标「Y/N」的配 <code>dictType: &quot;commonYN&quot;</code>（13 个，读登录缓存无需预加载）。两个实现细节：renderField 先判空（<code>raw == null || raw === &quot;&quot;</code> → &quot;-&quot;）再分流——getMdpDictLabel 对空值返回空串，不先判空会绕过 &quot;-&quot; 占位符；el-descriptions 是响应式渲染（vxe formatter 非响应式的坑在此不适用），但保持「先 await 字典再请求数据」的列表页同构范式，首屏不闪原码。用户原话「列表不是有说明，列表有的详情也要请求」——镜像页详情与列表的字典口径必须一致，否则验收必返工。</li><li><strong>字典加载与列表请求并行化（2026-09-03 五次增补，用户定型替换串行范式）</strong>：用户原话「列要计算属性，等接口返回相关字段再回显，而不是等待接口全部请求，以后这样的都得这样改」——旧范式「onMounted 中 await loadMdpDicts([...]) 后再 refresh()」让首屏串行等待全部字典接口，被否决。新范式（列表页 vxe）：onMounted 中 <code>refresh()</code> 立即发出列表请求 + <code>loadMdpDicts([...])</code> 并行（不 await）；字典就绪后 <code>.then()</code> 里 <code>const vxe = proTableRef.value?.tableRef; const rows = vxe?.getTableData()?.fullData || []; if (rows.length) vxe.reloadData(rows);</code>——<strong>同数据重载</strong>触发 vxe formatter 缓存重建（getCellLabel 按 rowid+colid 缓存、仅数据重载才重算），不重发请求、不改分页；若列表晚于字典返回，数据到达时 formatter 直接命中缓存（无竞态）。副作用：reloadData 重置勾选状态（低概率交互窗口，可接受）。详情页（el-descriptions 响应式渲染）更简单：字典与详情请求直接并行不 await，renderField 读 reactive 缓存依赖自动追踪，字典后到自动重渲——无需 reloadData（vxe 缓存机制仅适用表格）。defineExpose 暴露的 ref 经模板引用访问自动解包（<code>proTableRef.value?.tableRef?.</code> 直调 vxe 方法已有 6 页先例）。已同步修订 harness §8.1.2。</li></ol><h2 id="_10-契约数据版本滞后-pageschema-返回旧列集——先指纹定位滞后层再修-2026-09-03-增补�" tabindex="-1">10. 契约数据版本滞后：pageSchema 返回旧列集——先指纹定位滞后层再修（2026-09-03 增补�? <a class="header-anchor" href="#_10-契约数据版本滞后-pageschema-返回旧列集——先指纹定位滞后层再修-2026-09-03-增补�" aria-label="Permalink to &quot;10. 契约数据版本滞后：pageSchema 返回旧列集——先指纹定位滞后层再修（2026-09-03 增补�?&quot;">​</a></h2><blockquote><p>来源：商品主数据扩列�?86 列后实战——磁盘上的新 DML 未落库，/sys/pageSchema 仍只返回旧版 24 列，误以为接口少数据�?</p></blockquote><p><strong>机制</strong>：pageSchema 返回�?�?建模登记全量列，而是三层交集�?</p><p><code>sys_menu_form_column.visible_columns</code>（Redis Hash <code>sys:cache:menuForm:{tenantId}::menuForm</code>，field=menuCode，懒加载，直改库不失效）�?<code>sys_model_table_column</code> 实时列（FormColumnParser�? 分钟本地缓存）− 角色黑名单（admin 跳过）；id 不在绑定数组时补位首位�?</p><p><strong>判定特征（先定位滞后层，不要先怀疑代码）</strong>：返回列集与某个历史版本 DML �?visible_columns 数组**逐位一�?* + id 在首位（补位特征）→ 运行库停留在该版本。本例返�?24 �?= 08-27 �?23 列绑�?+ id 补位，缺 zsyear�?9-01 才加）、缺 subseries 等（09-03 才加），版本指纹直接指向 08-27 版数据�?</p><p>**修复序（任一层滞后都单独足以致旧�?*�?</p><ol><li>�?*后端实际连接的库**重跑最新幂�?DML（自�?DELETE 清理）；</li><li>SQL 指纹核验：<code>SELECT JSON_LENGTH(visible_columns) FROM sys_menu_form_column WHERE menu_code=&#39;xxx&#39; AND del_flag=0;</code>（应=绑定列数�? <code>SELECT COUNT(*) FROM sys_model_table_column c JOIN sys_model_table t ON t.id=c.table_id WHERE t.form_code=&#39;xxx&#39; AND c.del_flag=0 AND t.del_flag=0;</code>（应=元数据行数）�?</li><li>清绑定缓存：<code>HDEL sys:cache:menuForm:{tenantId}::menuForm {menuCode}</code>（或 DEL �?key）；</li><li>列元数据 5 分钟本地缓存等过期，或重启后端立即生效；</li><li>重新 curl /sys/pageSchema，核对列数与顺序�?</li></ol><p><strong>教训</strong>：磁盘上�?DML 文件 �?库里的数据；直改�?�?缓存失效。契约类接口的返回面是「绑定白名单 × 实时�?× 双层缓存」的交集，任何一层滞后都表现为「少数据」�?</p><hr><ol start="8"><li><strong>computed columns 终版：列配置本身就是响应式的「计算属性」（2026-09-03 六次增补，同日三代方案收敛）</strong>：用户追问「column 不能写计算属性？返回哪些字段就回显」——正确，且比 reloadData 更干净。vxe 4.20 源码实证：getCellLabel 缓存挂 rowRest.formatData[colid]、命中条件仅 <code>value === cellValue</code>，<strong>formatter 引用更新不失效缓存</strong>（watchColumn 对 formatter prop 变化只走 column.update，不触发刷新队列），故单纯 computed columns 也不会自动重算——缺的最后一块是 vxe 官方实例方法 <strong>clearFormatterCache(true)</strong>（清全部 label 缓存并立即重算，内部从未自调）。终版形态：页面 columns 写成 <code>computed&amp;lt;Column[]&amp;gt;</code>（getter 内 forEach 真实读取 getMdpDictList 的 reactive cache 建立依赖，字典清单提为 MDP_DICT_CODES 常量与 loadMdpDicts 共用单一来源）+ ProTable 内建 <code>watch(finalColumns, () =&gt; nextTick(() =&gt; tableRef.value?.clearFormatterCache?.(true)))</code>。链路：字典后到 → cache 变化 → columns 重算 → ProTable 清缓存重算回显；切语言同链路自动覆盖（cacheKey 带语言后缀 + td 标签也在 computed 内）。对比 reloadData 优势：不动数据/勾选/分页（clearFormatterCache 只重算显示）；页面无需持有 vxe 引用。静态列页面 watch 永不触发零开销，通用能力内建组件安全。</li></ol>',56)])])}const g=o(a,[["render",r]]);export{u as __pageData,g as default};
